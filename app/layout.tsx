import type { Metadata } from "next";
import { Fraunces, Inter, JetBrains_Mono } from "next/font/google";
import "./globals.css";
import SiteNav from "@/components/SiteNav";
import SiteFooter from "@/components/SiteFooter";

const display = Fraunces({
  subsets: ["latin"],
  variable: "--font-display",
  display: "swap",
});

const sans = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
  display: "swap",
});

const mono = JetBrains_Mono({
  subsets: ["latin"],
  variable: "--font-mono",
  display: "swap",
  weight: ["400", "500"],
});

export const metadata: Metadata = {
  title: {
    default: "Debelu Ventures — Software infrastructure for ambitious businesses",
    template: "%s — Debelu Ventures",
  },
  description:
    "Debelu Ventures designs and builds software, financial technology, intelligent systems, and digital infrastructure for organizations operating across complex industries.",
  metadataBase: new URL("https://debelu.ventures"),
  openGraph: {
    title: "Debelu Ventures — Software infrastructure for ambitious businesses",
    description:
      "Software, fintech, intelligent systems, and digital infrastructure for organizations operating across complex industries.",
    type: "website",
    images: [{ url: "/images/og-cover.png", width: 1200, height: 630 }],
  },
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={`${display.variable} ${sans.variable} ${mono.variable}`}>
      <body>
        <a
          href="#main"
          className="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-[100] focus:rounded-full focus:bg-accent focus:px-4 focus:py-2 focus:text-sm focus:text-white"
        >
          Skip to content
        </a>
        <SiteNav />
        <main id="main">{children}</main>
        <SiteFooter />
      </body>
    </html>
  );
}
